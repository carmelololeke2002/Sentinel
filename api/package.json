{
  "name": "antivirus-api",
  "version": "0.1.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "eslint --ext .js ./",
    "test": "echo \"No tests yet\" && exit 0",
    "healthcheck": "node -e \"require('./server').probe && console.log('ok')\""
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "nodemon": "^2.0.0"
  }
}
